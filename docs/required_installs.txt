installs

pip install customtkinter
pip install tkhtmlview
pip install pyserial (for serial library)
pip uninstall Pillow

For exe:
pip install auto-py-to-exe

(need "! auto-py-to-exe" to run at least on windows)